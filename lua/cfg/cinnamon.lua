require('cinnamon').setup {
    default_keymaps = true,
    extra_keymaps = true,
    extended_keymaps = true,

    centered = true,
    default_delay = 3,
    hide_cursor = true,
    max_length = 500,
    scroll_limit = -1,
}
